{
  "scripts": {
    "build": "slidev build --base /what-the-wasm/",
    "dev": "slidev --open",
    "export": "slidev export",
    "publish": "gh-pages -d dist"
  },
  "dependencies": {
    "@slidev/cli": "^0.30.0",
    "@slidev/theme-default": "^0.21.2"
  },
  "name": "what-the-wasm",
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
